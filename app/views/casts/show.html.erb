Name: <%= @cast.name %><br/>
Date of Birth: <%= @cast.date_of_birth %><br/>
Number of Movies: <%= @cast.no_of_movies %><br/>
Rank : <%= @cast.rank %><br/>

<br>

<h2>Movies Acted</h2>
<% @movies_acted.each do |movie| %>
	<%= link_to movie.name, movie %>
	<br>
<% end %>

<h2>Actors</h2>
<% @movies_directed.each do |movie| %>
	<%= link_to movie.name, movie %>
	<br>
<% end %>

<%= link_to 'Edit', edit_cast_path(@cast)  %>
<%= link_to 'Back', casts_path %>
<%= link_to "Edit", edit_cast_path(@cast) %>


<br>
<br>

